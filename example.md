# Basic device info
python main.py "what is my device model and manufacturer?"
python main.py "get battery level"
python main.py "show me the Android version"

# Shell commands
python main.py "list files in /sdcard"
python main.py "execute command: ls /system/app"
python main.py "check wifi status"

# Device management
python main.py "disconnect from device"
python main.py "show active device"
python main.py "list all connected devices"

# Combined queries
python main.py "tell me everything about my device"
python main.py "what apps are installed?"
python main.py "show me device storage info"

# Creative queries (test LLM reasoning)
python main.py "is my battery low?"
python main.py "what phone do I have?"
python main.py "show me system information"
